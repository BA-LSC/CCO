/** Cron for nightly PCO membership reconcile (`workers/reconcile-cron`). Cloudflare runs in UTC. */
export const PCO_NIGHTLY_RECONCILE_CRON = "0 3 * * *";

export const PCO_NIGHTLY_RECONCILE_SCHEDULE_LABEL = "Nightly at 3:00 AM UTC";
